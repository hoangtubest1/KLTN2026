{
  "name": "sports-booking-system",
  "version": "1.0.0",
  "description": "Hệ thống đặt lịch thể thao - Football, Badminton, etc.",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm install && REACT_APP_API_URL=/api npm run build && cd ../server && npm install",
    "start": "cd server && node index.js"
  },
  "keywords": ["sports", "booking", "react", "nodejs"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
